# Copy to .env (or export in your shell) to run with real Claude agents.
# Without a key, the office runs in demo mode.
ANTHROPIC_API_KEY=sk-ant-...
# Optional: change the port (default 3000)
# PORT=3000
